# 查詢使用者權限流程

## 觸發

瀏覽器端應用（外部系統）欲查詢「自身」（持有 token 的當前使用者）權限時，呼叫前端 SDK：先以 `getPerm(url, tokenTar, opt)` 直接取得 `{ user, rules }`，或以 `perm()` 工廠的 `conn(url)` 載入後用 `active(key)` / `getUser()` / `getRules()` 查詢單一規則或快取結果。

兩者最終都對後端 `/api/getPerm?token={token}` 發出 GET 請求：`getPerm.mjs` 以 `axios.get` 將 `url` 內 `{token}` 佔位符替換為實際 `tokenTar` 後送出（`src/getPerm.mjs:25-32`）；`perm.mjs` 的 `conn` 接收已帶妥 token 的完整 `url`，以 `axios.get` 送出（`src/perm.mjs:43`）。

後端 `/api/getPerm` handler 自 `req.query.token` 取 token，依序經 `getAndVerifyClientUser`（反查 perm `users` 表 + `verifyClientUser`）→ `getGenUserAndRulesByUserId`（查 `grups` / `pemis` / `targets` 並算 `getUserRules`）後，以 `pm2resolve` 包成 `{ state, msg }` 回傳（`server/WWebPerm.mjs:911-979`）。**token 無效 / 非 client 使用者 / 查無使用者一律 reject，`state` 回 `'error'`**。

## 重要流程

> 本流程為 query-api 類型：請求成功僅回傳 JSON 結構（`{ user, rules }`），無瀏覽器 UI 終態，故各 E2E 以 API 回傳結構 / `state` / 權限值為斷言對象，**不含 pixel baseline**。

- **E2E-001**
  - title: 持有效 token 的使用者查詢自身權限取得 user 與 rules
  - description: 驗證瀏覽器應用以有效 client 使用者 token 呼叫 `getPerm(url, tokenTar)`，後端依 token 解出該使用者、合併其群組與權限規則後，回傳含 `user`（含 `grupsNames`）與 `rules` 陣列的物件。屬 golden 成功路徑，其餘失敗 / 轉換案例皆以此回傳結構為基準。
  - flow:
    - 測試資料：base seed 既有 client 使用者（有對應 `grups` / `pemis` / `targets` 關聯）及其有效 token。
    - 操作：以該使用者 token 呼叫 `getPerm(url, tokenTar)`，url 含 `token={token}` 佔位符。
    - 驗證：
      1. 語意：回傳值為物件，含 `user` 與 `rules` 兩鍵；`user.id` 對應該 token 使用者、`user` 含 `grupsNames` 字串；`rules` 為陣列、各元素含 `name` 與 `isActive`。
      2. 視覺：純 API 無 UI 終態，無 pixel baseline。
    - 雙語：本流程為 API、回傳不含 i18n 文案，僅跑一輪。
    - 清理：本案唯讀，不改 DB，無需清理。

- **E2E-002**
  - title: 以 perm() 工廠 conn 載入後用 active 查詢單一規則
  - description: 驗證瀏覽器應用以 `perm()` 取得 SDK，`conn(url)`（url 已帶妥 token）載入成功後，`getUser()` / `getRules()` 回傳與 E2E-001 相同的 `user` / `rules`，且 `active(key)` 對既有規則名回傳 `'y'`（規則 `isActive='y'`）或 `'n'`（其餘 / 不存在）。屬 SDK 快取查詢成功案例。
  - flow:
    - 測試資料：base seed client 使用者與有效 token；該使用者至少一條 `isActive='y'` 規則與一個不存在的規則名。
    - 操作：`let p = perm()` → `await p.conn(url)` → 呼叫 `p.getUser()` / `p.getRules()` / `p.active(知名規則名)` / `p.active(不存在規則名)`。
    - 驗證：
      1. 語意：`getUser()` 回傳物件含該使用者 `id`；`getRules()` 回傳陣列；`active(已啟用規則名)` 回 `'y'`；`active(不存在規則名)` 回 `'n'`。
      2. 視覺：純 API 無 UI 終態，無 pixel baseline。
    - 雙語：API 流程，僅跑一輪。
    - 清理：本案唯讀，不改 DB，無需清理。

- **E2E-003**
  - title: token 無效時查詢被拒
  - description: 驗證以無效 / 偽造 / 過期 token 呼叫 `getPerm`，後端 `getAndVerifyClientUser` 反查不到對應 client 使用者，core reject，`pm2resolve` 將 `state` 設為 `'error'`；前端 `getPerm.mjs` 偵測 `state !== 'success'` 後 reject。屬 token 守門失敗案例，與 E2E-001 共同覆蓋成敗分支。
  - flow:
    - 測試資料：一組不對應任何有效 client 使用者的 token 字串。
    - 操作：以該無效 token 呼叫 `getPerm(url, tokenTar)`。
    - 驗證：
      1. 語意：`getPerm` 回傳的 Promise 被 reject，reject 字串含 `can not get user data by url`（後端 `state='error'` 觸發）；未取得 `user` / `rules`。
      2. 視覺：純 API 無 UI 終態，無 pixel baseline。
    - 雙語：API 流程，僅跑一輪。
    - 清理：本案被拒未寫 DB，無需清理。

- **E2E-004**
  - title: url 缺少 token 佔位符時前端即拒
  - description: 驗證呼叫 `getPerm` 時若傳入的 url 不含 `token={token}` 佔位符，`getPerm.mjs` 在送出 axios 請求「之前」即 reject，不打後端。屬前端參數前置檢查攔截案例。
  - flow:
    - 測試資料：一個合法但不含 `token={token}` 的 url（如 `http://host/api/getPerm`）與任意 tokenTar。
    - 操作：以該 url 呼叫 `getPerm(url, tokenTar)`。
    - 驗證：
      1. 語意：`getPerm` 回傳的 Promise 被 reject，reject 字串為 `no 'token={token}' in url`；無後端請求發出。
      2. 視覺：純 API 無 UI 終態，無 pixel baseline。
    - 雙語：API 流程，僅跑一輪。
    - 清理：本案前端即拒，無需清理。

- **E2E-005**
  - title: 提供 funConvertPerm 時回傳值經轉換
  - description: 驗證呼叫 `getPerm` 時於 `opt.funConvertPerm` 傳入轉換函式，後端回傳的 `{ user, rules }` 物件會先經該函式（同步或回 Promise 皆支援）轉換後才回傳；轉換後仍須為有效物件，否則 reject。屬回傳值客製轉換案例。
  - flow:
    - 測試資料：base seed client 使用者與有效 token；一個將回傳物件加上自訂欄位（仍為有效物件）的 `funConvertPerm`。
    - 操作：以 `getPerm(url, tokenTar, { funConvertPerm })` 呼叫。
    - 驗證：
      1. 語意：回傳值為經 `funConvertPerm` 轉換後的物件（含自訂欄位）；若 `funConvertPerm` 回非物件，`getPerm` reject、字串為 `no user data after funConvertPerm`。
      2. 視覺：純 API 無 UI 終態，無 pixel baseline。
    - 雙語：API 流程，僅跑一輪。
    - 清理：本案唯讀，不改 DB，無需清理。

## 執行流程

主要查詢路徑為前端 `getPerm`（`perm.mjs` 的 `conn` 為同款 axios 取得後再拆 `user` / `rules` / `kpRule` 的薄封裝，差異於回傳前的快取拆解，不另展開後端鏈）。

```
001  呼叫 getPerm(url, tokenTar, opt)  [getPerm.mjs:9]
002      檢查 url 為字串  [getPerm.mjs:14]
003          非字串:
004              reject('invalid url')  [getPerm.mjs:15]
005      檢查 tokenTar 為字串  [getPerm.mjs:17]
006          非字串:
007              reject('invalid tokenTar')  [getPerm.mjs:18]
008      取得 opt.funConvertPerm  [getPerm.mjs:22]
009      檢查 url 含 'token={token}' 佔位符  [getPerm.mjs:25]
010          不含:
011              reject(`no 'token={token}' in url`)  [getPerm.mjs:26]
012      以 tokenTar 取代 url 內 {token}  [getPerm.mjs:28]
013      呼叫後端 axios.get(url), reject 時於 catch 設 errTemp  [getPerm.mjs:32-35]
014          /api/getPerm handler 執行非同步流程 core()  [WWebPerm.mjs:914]
015              自 req.query.token 取 token  [WWebPerm.mjs:918]
016              檢查 token 為字串  [WWebPerm.mjs:922]
017                  非字串:
018                      reject('token does not have permission')  [WWebPerm.mjs:927]
019              呼叫 getAndVerifyClientUser(token, 'getPerm') 取發起者使用者  [WWebPerm.mjs:931]
020                  getTokenUser(token): 外部 getUserByToken 反查 + perm users 表正規化  [WWebPerm.mjs:594]
021                  verifyClientUser(user, from): 部署方識別是否為合法瀏覽使用者  [WWebPerm.mjs:597]
022                      不通過:
023                          reject('user does not have permission')  [WWebPerm.mjs:606]
024              檢查 userSelf 為物件  [WWebPerm.mjs:935]
025                  非物件:
026                      reject('token does not have permission')  [WWebPerm.mjs:939]
027              取得 userIdSelf = userSelf.id  [WWebPerm.mjs:943]
028              檢查 userIdSelf 為字串  [WWebPerm.mjs:947]
029                  非字串:
030                      reject('token does not have permission')  [WWebPerm.mjs:952]
031              呼叫 getGenUserAndRulesByUserId(userIdSelf)  [WWebPerm.mjs:956]
032                  getGenUserByUserId(userId) 反查使用者  [WWebPerm.mjs:782]
033                  查 grups / pemis / targets 三表  [WWebPerm.mjs:786,790,794]
034                  getUserRules(userFind, grups, pemis, targets) 合併算規則  [WWebPerm.mjs:798]
035                      (規則合併計算屬權限規則合併計算流程, 本文件不展開)
036                  檢查 kur 為物件  [WWebPerm.mjs:802]
037                      非物件:
038                          reject('can not get rules of user')  [WWebPerm.mjs:805]
039                  組 grupsNames (僅 isActive='y' 群組名以 ';' 串接)  [WWebPerm.mjs:809-810]
040                  回傳 { user: {...userFind, grupsNames}, rules }  [WWebPerm.mjs:812-819]
041              檢查 userWithRulesSelf 為物件  [WWebPerm.mjs:960]
042                  非物件:
043                      reject('token does not have permission')  [WWebPerm.mjs:966]
044              回傳 userWithRulesSelf  [WWebPerm.mjs:969]
045          pm2resolve(core) 將 resolve / reject 統一包為 { state, msg }  [WWebPerm.mjs:973]
046              core resolve: { state: 'success', msg: {user, rules} }
047              core reject: { state: 'error', msg: 錯誤字串 }
048          handler 回傳 r ({ state, msg })  [WWebPerm.mjs:979]
049      檢查 errTemp (axios 連線層錯誤)  [getPerm.mjs:38]
050          非 null:
051              reject(`can not get user by url[...]`)  [getPerm.mjs:42]
052      取得 data = res.data, state = data.state, msg = data.msg  [getPerm.mjs:46-52]
053      檢查 state 為 'success'  [getPerm.mjs:55]
054          非 success:
055              reject(`can not get user data by url[...]`)  [getPerm.mjs:61]
056      取 ur = msg ({user, rules})  [getPerm.mjs:65]
057      檢查 ur 為物件  [getPerm.mjs:69]
058          非物件:
059              reject(`no user data by url[...]`)  [getPerm.mjs:71]
060      檢查有提供 funConvertPerm  [getPerm.mjs:75]
061          有:
062              ur = funConvertPerm(ur) (回 Promise 則 await)  [getPerm.mjs:78-81]
063              檢查轉換後 ur 為物件  [getPerm.mjs:85]
064                  非物件:
065                      reject(`no user data after funConvertPerm`)  [getPerm.mjs:87]
066      回傳 ur  [getPerm.mjs:92]
```

## 回傳結構邊界

| 概念 | 內容 | 來源 |
|---|---|---|
| 後端 HTTP body | `{ state, msg }`：`state` ∈ `'success'` / `'error'` / `'cancelled'`；成功時 `msg` = `{ user, rules }`，失敗時 `msg` = 錯誤字串 | `pm2resolve` 統一包裝（`WWebPerm.mjs:973`） |
| `user` | `{ ...userFind, grupsNames }`：原使用者欄位加上 `grupsNames`（僅 `isActive='y'` 群組名以 `;` 串接） | `getGenUserAndRulesByUserId`（`WWebPerm.mjs:809-815`） |
| `rules` | 規則陣列，各元素含 `name`、`isActive`（`'y'` / `'n'`）等 | `getUserRules` 計算結果（`WWebPerm.mjs:818`） |
| `getPerm` 回傳 | 成功回 `ur`（即 `msg` 物件，或經 `funConvertPerm` 轉換後物件）；失敗回 reject 字串 | `src/getPerm.mjs:92` |
| `perm()` 快取 | `conn` 成功後將 `pmd` 拆為 `_user`（`pmd.user`）/ `_rules`（`pmd.rules`）/ `_kpRule`（`{ ruleName: isActive }`），供 `getUser` / `getRules` / `active` 讀取 | `src/perm.mjs:46-99` |
| `active(key)` | 讀 `_kpRule[key]`，值為 `'y'` 才回 `'y'`，其餘（含 key 不存在）回 `'n'` | `src/perm.mjs:117-121` |

## i18n 訊息粒度規則

本流程為 server-side 查詢 API，回傳 `{ state, msg }` 為**機器可讀值**（`state` ∈ `'success'` / `'error'` / `'cancelled'`，成功時 `msg` = `{ user, rules }` 物件、失敗時為固定英文錯誤字串）。**不經 i18n**——`server/procLang.mjs` 無對應鍵，由呼叫端應用自行決定如何呈現。故本流程無 i18n 訊息粒度規則，列此節僅為與其他流程文件章節對齊並載明緣由。

## 參數來源

| 概念 | 來源 | 流程影響 |
|---|---|---|
| `url` | 呼叫端傳入，須含 `token={token}` 佔位符 | `getPerm.mjs` 缺佔位符即前端 reject（`getPerm.mjs:25`）；`{token}` 由 `tokenTar` 取代後送 axios |
| `tokenTar` | 呼叫端傳入的當前使用者 token | 替換進 url 作為 `?token=` query；後端據此於 `getAndVerifyClientUser` 反查發起者使用者 |
| `opt.funConvertPerm` | 呼叫端可選傳入的轉換函式 | 後端回傳物件先經此函式轉換後才回傳；轉換後須為有效物件（`getPerm.mjs:75-89`） |
| token → 使用者 | 後端外部 `getUserByToken` + perm `users` 表（依 `mappingBy`，預設 `email`）反查正規化（`WWebPerm.mjs:537,549`） | 解出 `userIdSelf` 作為 `getGenUserAndRulesByUserId` 查權限的依據 |
| `verifyClientUser` | 部署方提供的識別函式 | 判定 token 對應使用者是否為合法瀏覽（client）使用者；不通過即 reject（`WWebPerm.mjs:597-606`） |
| 群組 / 權限 / 目標 | perm `grups` / `pemis` / `targets` 三表 | 由 `getUserRules` 合併計算出 `rules` 與有效 `grupsNames` |

## spec 規則摘要

- **權限**：`/api/getPerm` 守門點為 handler `core()` 內以 `req.query.token` 經 `getAndVerifyClientUser`（`getUserByToken` 反查 + perm `users` 表正規化 + `verifyClientUser` 部署方識別）；token 無效 / 非 client 使用者 / 查無使用者一律 reject，`state` 回 `'error'`。**僅限 client（瀏覽）使用者，不接受 app 使用者**（`WWebPerm.mjs:930`）。
- **查詢對象**：本端點固定查「token 自身所屬」使用者權限（`userSelf` → `userIdSelf`），呼叫端不可指定他人 userId。
- **回傳契約**：後端一律以 `pm2resolve` 回 `{ state, msg }`；前端 `getPerm.mjs` 以 `state === 'success'` 判成敗，成功取 `msg` 為 `{ user, rules }`，失敗 reject。
- **邊界**：
  - `url` 必含 `token={token}` 佔位符，否則 `getPerm` 在送請求前即 reject（前端前置檢查）。
  - `funConvertPerm` 為可選；提供時回傳物件先經其轉換，轉換結果須為有效物件才 resolve。
  - `grupsNames` 僅納入 `isActive='y'` 的群組名；`rules` 內各規則自帶 `isActive`，`perm().active(key)` 以此回 `'y'` / `'n'`。
  - `getUserRules` 的規則合併計算細節屬權限規則合併計算流程，本文件不展開。
- **契約**：前端 `getPerm` 以 `errTemp` 旗標短路 axios 連線層錯誤；後端 `core()` 經 `pm2resolve` 將 resolve / reject 統一轉為 `{ state, msg }` 供前端判讀。`perm().conn` 為同源 axios 取得後拆快取（`_user` / `_rules` / `_kpRule`）的薄封裝，成功 `permSuccess` / 失敗 `permError` 回呼可選。
